version: '3'

services:
  postgres:
    image: postgis/postgis:13-3.1-alpine
    container_name: mikro-orm-customtype-reproduction-postgres
    ports:
      - '5477:5432'
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres